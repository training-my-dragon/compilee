image: ubuntu/16.04
packages:
  - curl
sources:
  - https://github.com/training-my-dragon/compilee
tasks:
  - build-package: |
      cd compilee
      curl https://sh.rustup.rs -sSf | sh -s -- -y
      source "${HOME}/.cargo/env"
      cargo build
